# Internal notes — local only, never published
docs/

# Work-in-progress posts — local only until moved up to content/posts/
content/posts/drafts/

# Hugo build output
public/
resources/_gen/
.hugo_build.lock

# Hugo modules cache (if ever used)
_vendor/

# Editor / OS noise
.DS_Store
*.swp
